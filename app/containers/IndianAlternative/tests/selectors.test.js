// import { selectIndianAlternativeDomain } from '../selectors';

describe('selectIndianAlternativeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
